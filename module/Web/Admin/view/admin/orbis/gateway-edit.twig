<div class="gateway-edit accordion-group hide">
    <div class="accordion-heading">
        <div class="accordion-toggle text-center">
            {{ _('Edit gateway') }}
            {{ help('On minimap you can see selected gateway position -
                     you can drag and drop it where you want.') }}
            <button class="close">×</button>
        </div>
    </div>
    <div class="accordion-inner">
        {% do form.setAttribute('action', uri('orbis/default', {controller: 'gateways'}) ~ '/{item.id}') %}

        {{ bootstrap(form, 'horizontal') }}
    </div>
</div>